================================================================================
Task 7 — i18n 页面族迁移 batch 1：Chapters.tsx + Outline.tsx
Branch: feat/i18n-internationalization | refs #27
Date: 2026-09-01 | Worker: implementation (todo 7)
================================================================================

FILES CHANGED
--------------------------------------------------------------------------------
A frontend/src/locales/zh/chapters.json      (262 keys)
A frontend/src/locales/en/chapters.json      (262 keys)
A frontend/src/locales/zh/outline.json       (235 keys)
A frontend/src/locales/en/outline.json       (235 keys)
M frontend/src/i18n/resources.ts             (register chapters + outline NS imports)
M frontend/src/i18n/index.ts                 (ns array += 'chapters','outline')
M frontend/src/pages/Chapters.tsx            (all UI strings -> t(), 3170 lines)
M frontend/src/pages/Outline.tsx             (all UI strings -> t(), 2571 lines)
M frontend/src/types/resources.d.ts          (regenerated by i18n:types)

NOTE: resources.ts / index.ts / resources.d.ts also carry concurrent todo-8
(characters/settings) content — parallel Wave-2 worker committed first
(46c5ae6); this commit layers chapters/outline registration on top. Files were
re-read from the worktree before editing to merge cleanly.

KEY COUNTS (flat, dotted-path)
--------------------------------------------------------------------------------
chapters: zh 262, en 262  — identical key sets (python flat compare: 0 diff)
outline:  zh 235, en 235  — identical key sets (python flat compare: 0 diff)
Namespaces grouped by UI area: toast / batch / conflict / editor / editModal /
export / gate / generateModal / group / label / list / manual / notification /
page / pagination / perspective / plan / status / tag / unit (chapters);
batchExpand / editModal / expand / existing / export / generate / import /
label / listItem / manual / page / pagination / perspective / toast (outline).

PATTERN FOLLOWED (todo 6, commit 3a6a561)
--------------------------------------------------------------------------------
- const { t } = useTranslation('chapters') / useTranslation('outline')
  (defaultNS is 'common', so explicit NS arg required).
- Interpolated template strings -> t('key', {param: expr}) with {{param}} in JSON.
  Used {{n}} / {{number}} / {{total}} etc. instead of {{count}} to avoid i18next
  plural-key handling on the reserved `count` interpolation variable.
- Comments (// and {/* */}) kept Chinese — NOT translated.
- Dev console.* log Chinese messages translated to English inline (todo 6
  precedent: console.error('load current user failed:', error) in UserMenu).
- AI/user data NOT migrated: chapter body previews (item.content substring),
  plan_data.* (emotional_tone/conflict_type/key_events/...), outline
  content/structure fields, backend error_message strings, Select.Option
  VALUE="第一人称"/"第三人称"/"全知视角" (data sent to & stored from backend —
  only their display label is migrated), genre fallback '通用',
  narrative_perspective fallback '第三人称' (request payload values, not UI).
- task.error_message.includes('重试') kept: it inspects backend-generated
  text (user data), not UI chrome.

VERIFICATION RESULTS
--------------------------------------------------------------------------------
1. Bare-Chinese grep scan (regex on string literals + JSX text, comments and
   data-value exceptions excluded):
   Chapters.tsx: 7 hits, all whitelisted data values
     - '第一人称'/'第三人称'/'全知视角' object KEYs in getNarrativePerspectiveText
       lookup (keys are backend-persisted values; display mapped through t())
     - '重试' in task.error_message.includes() (backend message inspection)
     - Select.Option value="第一人称" x3 (data values, labels migrated)
   Outline.tsx: 6 hits, all whitelisted data values
     - '通用' genre request fallback, '第三人称' narrative_perspective request
       fallback x2, Select.Option value="第一人称|第三人称|全知视角" x3
   => 0 genuine UI string literals remain. CLEAN.
2. npm run build (tsc -b && vite build) ................. exit 0
   (only pre-existing chunk-size warnings; tsc type-checks every t() key
    against generated Resources type => all 262+235 keys verified to exist)
3. npm run i18n:types  then npm run i18n:types:ci ....... exit 0
   "✅ TypeScript definitions are up to date."
4. npm run i18n:extract then npm run i18n:extract:ci ..... exit 0
   "✅ No files were updated." (extract converged; zh is primaryLanguage so
   it is the type source of truth; removeUnusedKeys=false per config)
5. zh/en key-set parity (python flat compare):
   chapters zh 262 == en 262 (diff: none); outline zh 235 == en 235 (diff: none)
   Interpolation-param cross-check: zh/en {{param}} sets match for every key
   except outline.existing.confirmContentP1/P2, where {{title}} is intentionally
   placed in whichever segment reads naturally per language (same params object
   passed at call site — no runtime risk).
6. t()-key usage check: every t('...') literal resolves to an existing key;
   no seed key unreferenced (removed unused toast.aiCreatingClose).
7. eslint Chapters.tsx + Outline.tsx: 0 errors, 0 warnings after adding `t` to
   the pollActiveAnalysisTasks/analysis-gate useMemo dep arrays (gate map &
   batchAnalyzable deps corrected to include/remove t where actually used).
8. No dynamic keys: zero t(`...`) template-literal key constructions introduced
   (i18next-cli extract discovers 100% of keys via static literals).

MANUAL-QA NOTES (render Chapters / Outline pages)
--------------------------------------------------------------------------------
Setup for F3 (dev preview of the worktree build):
  - Backend serves the production static bundle built by `npm run build`
    (output -> backend/static). The long-running vite dev server on :5173 and
    uvicorn on :8008 in the MAIN checkout point at /Users/andyhsu/codehouse/
    AIFictionForge, NOT this worktree, so they do not reflect these edits.
    To see the migrated pages, serve the freshly built worktree bundle:
      cd backend && PYTHONPATH=../  <main-venv python> -m uvicorn app.main:app \
        --port 8018   # or run `npx vite preview` / `npm run dev` from THIS worktree
    then open http://localhost:8018 (or 5174) in a browser.
Happy path (F3-1):
  1. Language switcher -> 中文: Chapters & Outline pages render exactly the
     original Chinese copy (zh fallback == original literals).
  2. Switch to English: ALL chrome becomes English — page titles (章节管理->
     Chapters, 故事大纲->Story Outline), toolbar buttons, table/list column &
     group headers, form labels + placeholders, Modal titles & okText/cancelText,
     Popconfirm text, status tags (草稿->Draft, 已分析->Analyzed...), all toast
     messages (trigger save/export to observe interpolated values like
     "Queued 5 chapters ..."). No Chinese UI strings leak in en mode.
  3. Interpolation spot-check: export-confirm dialog shows the project title
     inside the English sentence; pagination shows "{{total}} total".
Failure path (F3-2 — missing key behavior):
  - Delete a key from en/chapters.json only, rebuild, switch to English and
    open Chapters: i18next falls back to zh (fallbackLng:'zh') so the text
    still renders in Chinese, and the dev console logs a
    "[i18next] Missing key" warning. Restore the key afterwards.
    (Same for outline.json.) No raw "chapters.toast.xxx" key strings surface.

DEVIATIONS / NOTES
--------------------------------------------------------------------------------
- console.* dev-log Chinese messages were translated to English inline rather
  than via t(): they are developer diagnostics, not user-facing UI, and todo 6
  established this exact convention (UserMenu/ChangelogModal). Kept the scan
  honest by leaving them out of the t() key space.
- InputNumber word-count formatter/parser now interpolate the unit through
  t('unit.words') ("字" / "words"); parser strips the localized suffix. The
  formatted numeric suffix is UI-only (form value stays a number).
- narrative-perspective Select display text is localized but the option VALUES
  remain the Chinese literals, because those exact strings are the persisted
  request/DB values (project settings use them). Backend English aliases
  (first_person/...) also still map, via the same lookup.
- Interpolated params named to avoid i18next plural reserved words ({{n}}
  instead of {{count}}).
- Worktree shared with concurrent Wave-2 todo-8 worker: an early untracked-file
  wipe forced recreation + `git add` of the four locale JSONs before continuing.
  Final tree contains BOTH todos' registrations; verification run over the
  combined state (extract/types/build all green).

================================================================================
RESULT: PASS — all 8 verification gates green. One commit pending:
  feat(i18n): migrate Chapters & Outline pages refs #27
================================================================================
