# Task 8 QA Evidence — i18n migration: Characters.tsx + Settings.tsx
Date: 2026-09-01
Branch/worktree: feat/i18n-internationalization @ /Users/andyhsu/codehouse/aff-i18n-internationalization

## Files changed
- frontend/src/locales/zh/characters.json   (NEW, 136 keys)
- frontend/src/locales/en/characters.json   (NEW, 136 keys)
- frontend/src/locales/zh/settings.json     (NEW, 191 keys)
- frontend/src/locales/en/settings.json     (NEW, 191 keys)
- frontend/src/i18n/resources.ts            (registered characters + settings namespaces)
- frontend/src/i18n/index.ts                (ns: ['common','errors','characters','settings'])
- frontend/src/pages/Characters.tsx         (206 t() call sites; useTranslation('characters'))
- frontend/src/pages/Settings.tsx           (215 t() call sites; useTranslation('settings'))
- frontend/src/types/resources.d.ts         (regenerated by i18next-cli types)
- frontend/src/locales/{zh,en}/errors.json  (re-sorted by i18next-cli extract; key sets unchanged)

## Migration scope notes
- UI chrome only: 43 form labels / 42 placeholders / 18 toasts (Characters), 29 toasts /
  24 buttons / 13 titles / labels+placeholders / 4 confirms (Settings), plus modals, tabs,
  import/export dialogs, validation messages, MCP-warning content.
- Language selector labels from todo 3 migrated to t(): settings.language.zhLabel ("简体中文")
  / settings.language.enLabel ("English"). Behavior untouched: selector still calls the same
  handleLanguageChange (i18n.changeLanguage + settingsApi.updatePreferences PATCH). The
  zh/en ternary toast branches were collapsed to t('language.updated') / t('language.syncFailed')
  — same effective message per active language (the old code already keyed the text off the
  selected language; i18n renders the string for the now-active UI language).
- Comments kept Chinese (Characters 24, Settings 21 JSX/line comments — untouched).
- Developer console.error/log messages rewritten to English (todo-6 precedent, not user-visible).
- Gender Select option `value="男|女|其他"` kept as user data (sent to backend); visible labels
  migrated to t('gender.*'). Role option values (protagonist/supporting/antagonist) likewise kept.
- Interpolations via i18next {{n}} params (count renamed to n to avoid i18next v26 plural-key
  auto-expansion creating _one/_other variants, which would break strict zh/en key parity).
- Dynamic detail fallbacks preserved: `apiError.response?.data?.detail || t('toast.importFailed')` etc.
- AI-generated content display not touched (no such rendering in these two files' migrated paths).

## Verification results (all inside worktree frontend/)
1. Bare-Chinese grep (comment-stripped, regex over whole file):
   - Settings.tsx: 0 lines with Chinese literals outside comments.
   - Characters.tsx: 6 hits, all intentional data-value attributes `value="男|女|其他"`
     (backend enum values; their visible children ARE migrated). No bare UI literals.
2. npm run build (tsc -b && vite build)            -> exit 0 (✓ built in 4.24s)
3. npm run i18n:types && npm run i18n:types:ci     -> exit 0 ("TypeScript definitions are up to date")
4. npm run i18n:extract && npm run i18n:extract:ci -> exit 0 ("No files were updated")
5. Flat key-set parity (python compare):
   - characters: zh=136 en=136 symmetric-diff=NONE
   - settings:   zh=191 en=191 symmetric-diff=NONE
6. git status --short after commit -> clean (see commit hash below)

## Manual-QA notes (for F3 human pass — steps)
Render check in browser (frontend dev on :5173, backend :8008):
a. Open 项目 -> 角色与组织管理 (Characters page):
   - Header/按钮: 创建角色 / 创建组织 / AI生成角色 / AI生成组织 / 导入 all render.
   - Tabs 全部(N)/角色(N)/组织(N) counts interpolate correctly.
   - 批量选择: 全选 -> 已选 N 个 -> 取消选择; 批量导出 (N) appears after selection.
   - 创建角色 modal: all 43 labels/placeholders render; validation errors show on empty submit
     (请输入角色名称 / 请选择副职业 / 阶段).
   - 编辑角色/组织 modal: career select shows 职业名（最高N阶）; gender dropdown shows
     男/女/其他 labels while persisting original Chinese values on save (verify PATCH payload
     gender value unchanged after language switch).
   - AI生成角色/组织 confirm modal: form labels + 生成/取消 buttons.
   - 导入 modal: title/说明 bullets; pick an exported JSON -> 导入预览 shows 文件版本/将要导入/
     角色: N 个/组织: N 个 -> 确认导入 -> 导入完成 summary (✅/⚠️/❌ counts).
b. Switch to English via Settings 界面语言 selector:
   - All of the above re-render in English; re-edit a Chinese-gendered character -> stored
     gender value remains "男" (data not translated).
c. Settings page:
   - Header 标题/副标题, 界面语言 card (label/description/zh & en options), tabs 文本模型配置/
     图片模型配置/配置预设.
   - Language selector STILL WORKS after migration: switch to English -> UI flips immediately,
     toast "Interface language updated"; check PATCH /settings/preferences payload contains
     {"language":"en"}; reload -> preference persists (todo-3 acceptance re-check).
   - 测试连接 flow: warnings 请先填写完整的配置信息, success 测试成功！响应时间: Nms (en: Test
     passed! Response time: Nms).
   - 配置预设 tab: 新建预设/从当前创建 modal labels, 激活/测试/编辑/删除 actions, preset test
     success/failure modals, chapter-analysis preset card + alert texts.
   - MCP auto-disable warning (if plugins active + provider changed): title/body/steps render.

## Deviations / discoveries
- i18next v26 (i18next-cli 1.72) auto-expanded {{count}} keys into _one/_other plurals during
  extract, breaking zh/en parity. Resolution: interpolation param renamed {{count}} -> {{n}}
  (call sites t(..., { n: ... })). Semantics unchanged; avoids plural-inflection entirely
  (Chinese has no plural; English strings written plural-neutral).
- Worktree shared with parallel todo-7 worker (Chapters/Outline): its untracked
  migrate_chapters.py + chapters/outline locale JSONs and its extract-side touch of
  chapters.json were parked to /var/folders/.../todo7-park/ before this commit so todo 8
  contains ONLY its own files. resources.ts / index.ts / resources.d.ts diffs contained only
  todo-8 namespace registrations at commit time (todo-7 has not registered its namespaces yet).
  TODO-7 worker must restore: cp -r /var/folders/q5/rrt3vpzs0jj15c168cnngpd80000gn/T/opencode/todo7-park/migrate_chapters.py
  <worktree>/frontend/ and the 4 JSONs to frontend/src/locales/{zh,en}/, then re-run extract
  (their files are unmodified apart from being extracted; zh/chapters.json had been touched by
  a shared extract run while parked artifacts existed).
